import React from 'react';

export default const CategorySelector = (props) => {
    return (
        <section>
            <h1>{props.game}</h1>
            Choose a Category
        </section>
    );
}